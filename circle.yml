machine:
  node:
    version: 8.9.3

dependencies:
  cache_directories:
    - "node_modules"

deployment:
  release:
    tag: /^v?\d+\.\d+\.\d+(?:-beta\.\d+)?$/
    owner: Financial-Times
    commands:
    - make npm-publish
